# Optional — used by the Python browser-use runner
BROWSER_USE_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Required — Supabase project used to persist config, applied history, and run state
# Get these from Supabase dashboard → Project Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
